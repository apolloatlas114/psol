// src-backend/index.ts
import express from "express";
import path from "path";
import { fileURLToPath } from "url";
import { getAPI } from "./api.mjs";
import "./ext/Room.mjs";
var import_meta = {};
var getDirname = () => typeof __dirname !== "undefined" ? __dirname : path.dirname(fileURLToPath(import_meta.url));
var frontendDirectory = path.resolve(getDirname(), "..", "build", "static");
function monitor(opts = {}) {
  const router = express.Router();
  router.use(express.static(frontendDirectory));
  router.use("/api", getAPI(opts));
  return router;
}
export {
  monitor
};
